// we don't need to require mongoose cause we are pulling it in from the 'dal'
const Partners = require('../dal_data_access_layer/partners.dal');

exports.createPartner = function(req, res, next)
{
     
}

